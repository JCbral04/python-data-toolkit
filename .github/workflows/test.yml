name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          # cache: 'pip' 

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff mypy pyarrow # TEST CON pyarrow
          pip install -e ".[dev]"

      - name: Run linting
        run: |
          python -m ruff check src/
          python -m mypy src/ --python-version ${{ matrix.python-version }} # Test para evitar que mypy falle en 3.12+ con la sintaxis de NumPy

      - name: Run tests
        run: python -m pytest tests/ -v
